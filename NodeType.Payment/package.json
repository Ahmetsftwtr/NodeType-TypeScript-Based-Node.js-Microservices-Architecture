{
    "name": "NodeType.Payment",
    "version": "1.0.0",
    "description": "A server application using TypeScript and Express",
    "main": "dist/server.js",
    "scripts": {
        "build": "tsc",
        "start": "npm run build && node dist/NodeType.Payment/src/server.js",
        "dev": "npm run build && npm run start"
    },
    "dependencies": {
        "express": "^4.19.2",
        "pg": "^8.12.0",
        "typeorm": "^0.3.20"
        "rimraf": "^6.0.1",
    },
    "devDependencies": {
        "@types/express": "^4.17.21",
        "@types/node": "^22.3.0",
        "rimraf": "^6.0.1",
        "typescript": "^5.5.4"
    }
}
